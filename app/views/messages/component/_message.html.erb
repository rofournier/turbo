<div id="<%= dom_id(message) %>" class="flex items-center space-x-2">
  <div class="flex items-center space-x-2">
    <span class="font-semibold"><%= message.sender %></span>
  </div>
  <span><%= message.body %></span>
</div>
